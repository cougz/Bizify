// This file will be injected into the HTML at build time
// It can be overridden by the deployment environment
window.RUNTIME_CONFIG = {
  // Default API URL (can be overridden at deployment time)
  API_URL: '/api',
  // Base path for the application (useful when deployed under a subdirectory)
  BASE_PATH: '/',
};
